#include <stdio.h>
/**
 * main - entry point
 *
 * Description: prints numbers
 *
 * Return: 0
 */
int main(void)
{
	int num = 0;

	while (num < 10)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
