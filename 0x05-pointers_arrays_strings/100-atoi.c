#include "main.h"
#include <string.h>
#include <stdlib.h>
/**
 * _atoi - change string to int
 * @s: passed string
 *
 * Return: the number
 */
int _atoi(char *s)
{
	int num = atoi(s);

	return (num);
}
