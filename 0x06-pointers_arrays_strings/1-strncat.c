#include "main.h"
/**
 * _strncat - concatinats strings
 * @src: fst
 * @dest: scnd
 * @n: bytes
 *
 * Return: pointer
 */
char *_strncat(char *dest, char *src, int n)
{
	return (strcat(dest, src, n));
}
